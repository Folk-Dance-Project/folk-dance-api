const GROUP_MEMBERSHIP_ROLES = {
    ADMIN: "admin",
    MEMBER: "member",
};

const GROUP_MEMBERSHIP_STATUS = {
    APPROVED: "approved",
    PENDING: "pending",
    DENIED: "denied",
};

module.exports = {
    GROUP_MEMBERSHIP_ROLES,
    GROUP_MEMBERSHIP_STATUS,
};
